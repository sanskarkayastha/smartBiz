# NeonDB connection strings
# Get these from your NeonDB project dashboard (neon.tech)
# Each service gets its own database within the same NeonDB project
# Format: jdbc:postgresql://<host>.neon.tech/<dbname>?sslmode=require

AUTH_DB_URL=jdbc:postgresql://<host>.neon.tech/auth_db?sslmode=require
INVENTORY_DB_URL=jdbc:postgresql://<host>.neon.tech/inventory_db?sslmode=require
CRM_DB_URL=jdbc:postgresql://<host>.neon.tech/crm_db?sslmode=require
SALES_DB_URL=jdbc:postgresql://<host>.neon.tech/sales_db?sslmode=require

# NeonDB credentials (same user across all databases in a project)
DB_USERNAME=neondb_owner
DB_PASSWORD=npg_OmDWhbzF80uP

# JWT secret — change this to a strong random string in production
JWT_SECRET=your-secret-key-change-in-production-at-least-32-characters-long

# Cloudinary product images (backend only; never expose the API secret to clients)
CLOUDINARY_ENABLED=false
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
CLOUDINARY_UPLOAD_PRESET=smartbiz_product_images

# Email verification (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
AUTH_EMAIL_ENABLED=true
AUTH_EMAIL_FROM=your-email@gmail.com
AUTH_EMAIL_DEV_LOG_OTP=false
AUTH_VERIFICATION_CODE_EXPIRY_MINUTES=10

# Google sign-in
AUTH_GOOGLE_ENABLED=true
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
AUTH_PUBLIC_BASE_URL=http://localhost:8080
GOOGLE_ALLOWED_REDIRECT_PREFIXES=http://localhost:3000/api/auth/google/callback,mobile://auth/callback

# Internal service authentication. Generate one long random value and use the same value in every service.
INTERNAL_SERVICE_TOKEN=replace-with-a-long-random-service-token

# Billing return and callback locations
BILLING_PUBLIC_BASE_URL=http://localhost:8080
BILLING_WEB_RETURN_URL=http://localhost:3000/dashboard/settings
BILLING_MOBILE_RETURN_URL=smartbiz://billing/result

# SmartBiz plan purchases through eSewa UAT
ESEWA_BILLING_ENABLED=true
ESEWA_SMARTBIZ_PRODUCT_CODE=EPAYTEST
ESEWA_SMARTBIZ_SECRET_KEY=8gBm/:&EnhH.1/q
ESEWA_FORM_URL=https://rc-epay.esewa.com.np/api/epay/main/v2/form
ESEWA_STATUS_URL=https://uat.esewa.com.np/api/epay/transaction/status/

# Shop counter eSewa credentials are encrypted with this server-only master key.
ESEWA_ENVIRONMENT=UAT
PAYMENTS_PUBLIC_BASE_URL=http://localhost:8080
ESEWA_CREDENTIAL_ENCRYPTION_KEY=replace-with-a-long-random-encryption-master-key
ESEWA_INTENT_BOOK_URL=https://rc-checkout.esewa.com.np/api/client/intent/payment/book
ESEWA_INTENT_STATUS_URL=https://rc-checkout.esewa.com.np/api/client/intent/payment/status
ESEWA_INTENT_CANCEL_URL=https://rc-checkout.esewa.com.np/api/client/intent/payment/cancel
ESEWA_UAT_PRODUCT_CODE=INTENT
ESEWA_UAT_ACCESS_KEY=LB0REg8HUSw3MTYrI1s6JTE8Kyc6JyAqJiA3MQ==

# Stripe remains test-only and disabled until SmartBiz has an eligible merchant account.
STRIPE_ENABLED=false
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me
